{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build:client",
  "outputDirectory": "dist/spa",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
